Import('env')

if env['ARCH'] == 'host':
    dirs = ['base', 'm3', 'test', 'thread']
else:
    dirs = ['c', 'base', 'm3', 'support', 'test', 'thread', 'hash']

for d in dirs:
    env.SConscript(Glob(d + '/SConscript'), 'env')
